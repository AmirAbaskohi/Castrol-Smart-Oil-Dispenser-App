export enum ProcedureType {
  None = 'none',
  Prime = 'prime',
  Calibrate = 'calibrate',
};

export enum ActionType {
  None = 'none',
  Start = 'start',
  Stop = 'stop',
};

export enum Pump {
  None = 'none',
  Left = 'left',
  Right = 'right',
};